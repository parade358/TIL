CREATE TABLE UTB_IMAGE (
    IMAGE_ID INT IDENTITY(1,1) PRIMARY KEY,
    IMAGE_NAME NVARCHAR(255) NOT NULL,
    IMAGE_BASE64 NVARCHAR(MAX) NOT NULL,
    CREATE_DTTM DATETIME DEFAULT GETDATE()
);

DROP TABLE dbo.UTB_IMAGE;